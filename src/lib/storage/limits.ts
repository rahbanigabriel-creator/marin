/** Product file limit for private creative media. */
export const MAX_ASSET_BYTES = 30 * 1024 * 1024;

/** Keep server-routed multipart bodies below Vercel's 4.5 MB function limit. */
export const MAX_SERVER_ASSET_BYTES = 4 * 1024 * 1024;
